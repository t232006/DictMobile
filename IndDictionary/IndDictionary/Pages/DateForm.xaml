<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="IndDictionary.Pages.DateForm"
             xmlns:loc="clr-namespace:IndDictionary.Converters"
             WidthRequest="150">
    <ContentPage.Resources>
        <ResourceDictionary>
            <loc:DateToString x:Key="dateToString"/>
        </ResourceDictionary>
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>
            <Label x:Name="Header" Text="Select date"/>
            <ListView x:Name="DatesList" RowHeight="40" Margin="10" SeparatorColor="Red" ItemTapped="onSelect">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <SwitchCell Text="{Binding Date}" On="{Binding Spoted, Mode=TwoWay}">
                        </SwitchCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>