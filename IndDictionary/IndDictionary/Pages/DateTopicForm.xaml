<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
<<<<<<< Updated upstream
             x:Class="IndDictionary.Pages.DateTopicForm">
    
=======
             x:Class="IndDictionary.Pages.DateTopicForm"
             xmlns:res="clr-namespace:IndDictionary.addition"
             Title="Select the dates or topics">

    <ContentPage.ToolbarItems>
        <ToolbarItem Clicked="OnApplayPress" 

                     
             IconImageSource="{res:ImageResourceExt IndDictionary.Resources.ok.png}"
             x:Name="EditBut"/>

        <!--ToolbarItem Text="Cancel" Clicked="OnCancel" Priority="1" Order="Primary"/-->
    </ContentPage.ToolbarItems>
>>>>>>> Stashed changes
    <ContentPage.Content>
        <StackLayout>
            <Label x:Name="Header" Text="Select the dates"/>
            <StackLayout Orientation="Horizontal">
                <Button Text="Cancel" Pressed="onCancelPress"/>
                <Button Text="Apply" Pressed="onApplyPress"/>
            </StackLayout>
            <ListView x:Name="DataList" RowHeight="40" Margin="10" SeparatorColor="Red" ItemTapped="onSelect" ItemsSource="{Binding}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <SwitchCell Text="{Binding DaOrTo}" On="{Binding Spoted, Mode=TwoWay}"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>